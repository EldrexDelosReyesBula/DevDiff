# Plugin API

Write custom middleware, watcher, or format extensions.

- **`PipelineStep`**: Custom stage inside the processor engine.
- **`CustomOutputPlugin`**: Deliver raw or formatted outputs to custom platforms.
