# Jira Integration

Map Jira issues and update comments with AI summaries.

## Setup

Jira is supported by standard webhook formatting hooks. Set up Jira webhooks directed to the DevDiff Gateway `/webhooks/jira` endpoint.
